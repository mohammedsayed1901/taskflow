# Host port used by Docker Compose.
APP_PORT=5000

# Replace with a unique cryptographically random value of at least 32 characters.
JWT_SECRET=replace-with-a-random-value-at-least-32-characters

JWT_EXPIRES_IN_SECONDS=604800
BCRYPT_SALT_ROUNDS=12